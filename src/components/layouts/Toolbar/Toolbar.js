import React from 'react';
import './Toolbar.scss';

const toolbar = (props) => (
  <header className='toolbar'>
    <div>MENU</div>
    <div>LOGO</div>
    <nav>
      ...
    </nav>
  </header>
);

export default toolbar;
